# PROTOTYPE：全屏动态唱机桌面

本目录是 Issue #12 的一次性视觉原型，用来回答：CloudPlatter 的全屏桌面场景应该采用哪种
构图和信息层级？它不连接真实播放数据，也不能直接作为生产实现合并。

运行：

```bash
make design-prototype
```

打开 <http://127.0.0.1:4173/?variant=A>，使用底部切换器或左右方向键查看 A、B、C 三种方案。
选型完成后，应把结论记录到 Issue #12，删除未采用方案，再按正式工程规范实现获选方向。
