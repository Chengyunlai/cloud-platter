# CloudPlatter 开发与贡献规范

感谢参与 CloudPlatter。本规范适用于代码、文档、测试、提交和发布流程。

## 参与方式

- 首个稳定版本发布前，仅接受维护者邀请的 contributor 提交 Pull Request。
- 未受邀贡献者可以创建 Issue 报告问题或提出建议；请不要直接提交实现 PR。
- 受邀 contributor 在开始编码前应先认领对应 Issue，并确认验收标准和阻塞项。
- 公共仓库无法阻止用户创建 PR，维护者会关闭未经协商的提交，但欢迎把背景和建议保留在 Issue 中继续讨论。

## 开发环境

- macOS 14 或更新版本。
- 完整 Xcode 及其包含的 Swift 工具链；只有 Command Line Tools 时可以构建应用，但可能缺少 Swift Testing 运行库。
- 首次克隆后运行 `make check` 验证格式、构建和测试。
- 运行 `make package VERSION=0.1.0-dev` 可在 `dist/` 生成 ad-hoc 签名的 Universal 应用 ZIP 和 SHA-256。

## 1. 语言规范

- `README.md` 是默认入口，必须使用中文。
- 英文说明放在 `README.en.md`，并在两份 README 顶部互相链接。
- 中文文档是内容基准；修改项目能力、安装方式或风险说明时，应在同一个 Pull Request 中同步英文版。
- Swift 类型、变量、函数、协议、文件名等代码标识符使用清晰的英文。
- **所有开发中的代码注释使用中文**，包括普通注释、文档注释、`TODO` 和测试中的解释性注释。
- 注释应解释设计原因、平台限制和不明显的约束，不要逐句翻译代码行为。
- API、框架、类型名和代码片段保留其官方英文名称，避免生硬翻译。

示例：

```swift
// MediaRemote 是未公开接口，调用失败时必须降级为空闲状态，不能让应用崩溃。
guard let nowPlayingInfo else {
    return .idle
}
```

## 2. Swift 代码规范

- 技术验证阶段优先使用 Swift 6，并以 macOS 14 作为暂定的最低验证基线；正式支持范围在 Spike 完成后确定。
- 缩进使用 4 个空格，不使用制表符。
- 类型和协议使用 `UpperCamelCase`；变量、函数和枚举成员使用 `lowerCamelCase`。
- 布尔值使用 `is`、`has`、`can` 或 `should` 等前缀表达语义。
- 默认避免强制解包和 `try!`；如果由系统不变量保证安全，必须用中文注释说明依据。
- UI 状态更新明确运行在 `MainActor`；异步事件优先使用 Swift Concurrency。
- 一个文件优先只承载一个主要类型；扩展按职责拆分，避免形成巨型文件。
- 新增公开类型和重要协议时，使用中文文档注释说明职责、边界和失败行为。

## 3. 架构边界

- 未公开的 MediaRemote 符号和字典键必须封装在独立适配层，禁止散落到 UI 或业务代码中。
- 渲染层只依赖项目定义的 `NowPlayingState`，不得直接依赖系统私有字典。
- 为播放来源建立满足当前测试需要的最小接口，便于使用伪造或录制状态验证 UI；在出现第二个实际来源前不预设额外扩展点。
- 首版保持只读：不注入网易云音乐进程、不抓取 Cookie、不模拟登录、不调用社区逆向 API。
- 私有接口必须使用运行时能力检测；接口不可用、字段缺失或格式变化时应安全降级。
- 引入第三方依赖前，应说明必要性、许可证、包体和长期维护成本。

## 4. 隐私与日志

- 默认不上传播放历史、曲名、艺人、封面 URL、账号信息或本地路径。
- 诊断日志不得记录完整的用户播放内容；确有需要时使用脱敏值，并让用户主动开启。
- 新增任何网络请求、遥测或权限前，必须在 Pull Request 中说明数据流和用途，并更新隐私说明。
- MediaRemote helper 的原始输出不得写入日志；默认只记录字段是否存在、字段类型和错误分类。
- Accessibility 与 ScreenCaptureKit 只能作为用户主动开启的备用能力，不能在首次启动时无说明地请求。

## 5. 测试与质量

- 状态解析、播放状态转换和异常降级必须有单元测试。
- UI 和动画测试使用伪造或录制的 `NowPlayingState`，避免依赖真实网易云音乐进程。
- 修复缺陷时优先补充能复现问题的测试。
- 提交前运行项目已有的格式检查、构建和测试命令；在工具尚未建立前，至少保证 Xcode/Swift 构建通过。
- 动画实现应验证窗口不可见、屏幕锁定和 Reduce Motion 场景，避免无意义的持续刷新。

## 6. Git 与 Pull Request

- 从 `main` 创建短生命周期分支，保持改动范围单一。
- 提交信息使用 Conventional Commits 类型，摘要可以使用中文，例如：`docs: 增加开发规范`。
- 常用类型包括 `feat`、`fix`、`docs`、`test`、`refactor`、`build` 和 `chore`。
- Pull Request 应说明目标、实现方式、验证结果、已知风险和界面变化；视觉改动附截图或录屏。
- 不提交生成目录、个人 Xcode 配置、签名证书、密钥或账号数据。

## 7. 发布规范

- 发布包通过 GitHub Releases 提供，并附版本说明和 SHA-256 校验值。
- 发布标签必须由仓库维护者创建，并指向 `main` 分支历史中的提交。
- 未签名或未公证的构建必须在下载页明确提示 Gatekeeper 行为和官方的手动允许步骤。
- 不要求用户全局关闭 Gatekeeper，也不把关闭系统安全机制作为标准安装方法。
- 每次发布记录已验证的 macOS 和网易云音乐版本，以及已知的 MediaRemote 兼容问题。
